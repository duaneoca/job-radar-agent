# Install:  pip install pre-commit && pre-commit install
# Runs gitleaks locally before every commit to keep secrets out of history. [M3]
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
